import { Card, CardContent } from "@/components/ui/Card";

export default function DriverHomePage() {
  return (
    <Card>
      <CardContent className="text-sm text-ink-muted">
        Today&rsquo;s delivery route will appear here once Route
        optimization (Phase 4) and Driver View (Phase 5) are wired up.
      </CardContent>
    </Card>
  );
}
